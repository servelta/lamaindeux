# Copy to .env.test.local and fill in with your LOCAL Supabase stack's
# values (run `supabase start` — it prints these). Never point integration
# tests at a production project: they create and delete real auth users.

NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
